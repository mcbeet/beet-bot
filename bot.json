{
  "database": {
    "type": "json",
    "path": "db.json"
  },
  "environments": {
    "default": {
      "warmup": 4,
      "timeout": 3000,
      "path": "docker/runner-default",
      "overrides": ["meta.json_reporter.binary_files = true"]
    }
  }
}
